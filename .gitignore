# IDE
.idea/
.vscode/

# env / secrets
.env
.env.local
*.env

# binaries (anchored to the root: otherwise it also matches cmd/mcp-ssh-fleet/)
/mcp-ssh-fleet
*.exe
*.so

# ad-hoc shell in the root (proper scripts live under scripts/)
/*.sh

# test artifacts
*.out
coverage.*
*.test

# vendor is committed in full
!vendor/**

# local keys/inventory for manual testing — NEVER commit
id_ed25519
id_ed25519.pub
*.pem
inventory.local.yaml

# OS
.DS_Store
Thumbs.db

# AI tool configs
.claude
.specify
specs
.cursor
.cursorrules
.windsurf
.aider*
.gemini
CLAUDE.md
AGENTS.md
GEMINI.md
PLAN.md

# Local-only git hooks (contain a specific private e-mail address)
.githooks/
